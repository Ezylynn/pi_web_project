{
    "dev": {
        "driver": "pg",
        "host": "${DB_HOST}",
        "port": "${DB_PORT}",
        "database": "${DB_DATABSE}",
        "user": "${DB_USER}",
        "password": "${DB_PASSWORD}"
    }
}